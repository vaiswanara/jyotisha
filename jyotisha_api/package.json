{
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "postinstall": "node -e \"const p=require('path'),cp=require('child_process');cp.fork(p.join(p.dirname(process.env.npm_package_json||'.'),'build-swetest.js'));\""
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2",
    "suncalc": "^1.9.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.2",
    "@types/suncalc": "^1.9.2",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
